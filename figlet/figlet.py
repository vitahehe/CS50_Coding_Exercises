import pyfiglet
import sys

try:
    if len(sys.argv) == 0 or len(sys.argv) == 2:
        if len(sys.argv) == 0:
            re pyfiglet.figlet_format(sys.a, font = "3-d" )


except ValueError:
    sys.exit()
